export const createAgentHTML = (agent) => {
return `
    <section class="agent">
            <h2 class="agent-name">${agent}</h2>
            <div class="address">
               
            </div>
        </section>
        `
}